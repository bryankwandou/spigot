node_modules/
.next/
out/
build/
.DS_Store
*.pem

# Local env — never commit. Keys live in Vercel and GitHub secrets only.
.env
.env.local
.env*.local

# Anything holding a keypair, in any form.
*.key
*keypair*.json
wallet*.txt

.vercel
*.tsbuildinfo
next-env.d.ts
